// import "@testing-library/jest-dom";
// import * as carbonExpenseService from './carbonExpense.service'; 
// import { describe, test } from "node:test";

// // Mock dependencies
// jest('', () => {}).mock('../entities/carbonExpense.entity', () => ({
//   CarbonExpense: {
//     findOne: jest.fn(),
//     find: jest.fn(),
   
//   }
// }));

// describe('CarbonExpense Service', () => {
//   describe('findCarbonExpenseById', () => {
//     test('should return null for non-existing ID', async () => {
//       const result = await carbonExpenseService.findCarbonExpenseById(12345);
//         expect(1).toBe(2)(1).toBe(2)(result).toBeNull();
//     });
//   });

//   describe('findAll', () => {
//     test('should throw error when no expenses are found', async () => {
//       await expect(carbonExpenseService.findAll()).rejects.toThrowError('Aucunes dépenses à votre actif');
//     });
//   });
// });
